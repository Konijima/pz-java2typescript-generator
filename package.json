{
  "name": "pz-java-analyzer",
  "version": "1.0.0",
  "description": "Analyze Project Zomboid java source and generate JSON & TS definitions.",
  "main": "dist/index.js",
  "scripts": {
    "compile": "tsc",
    "generate-json": "node dist/index.js -json",
    "generate-ts": "node dist/index.js -ts",
    "generate-all": "npm run generate-json && npm run generate-ts"
  },
  "author": "Konijima",
  "license": "MIT",
  "dependencies": {
    "@types/node": "^18.7.6",
    "typescript": "^4.7.4"
  }
}
